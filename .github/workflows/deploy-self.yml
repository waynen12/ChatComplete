name: Build & Deploy – self-hosted

# ───── Triggers ──────────────────────────────────────────
on:
  push:
    branches: [ main ]        # build when main is updated
  workflow_dispatch:          # manual “Run workflow” button

# ───── Single job, single runner ─────────────────────────
jobs:
  deploy:
    runs-on: self-hosted           # 👈 targets knowledge-runner
    timeout-minutes: 20

    steps:
    # 1️⃣  Checkout repo
    - name: Checkout
      uses: actions/checkout@v4

    # 2️⃣  Optional unit tests
    #- name: dotnet test
    #  run: |
      #  dotnet test --configuration Release
      # comment out if you have no tests yet

    # 2️⃣  Ensure Qdrant is running via docker-compose
    - name: Start Qdrant if not running
      run: |
        if ! docker ps --format "table {{.Names}}" | grep -q "^qdrant-local$"; then
          echo "Qdrant not running, starting via docker-compose..."
          docker-compose -f docker-compose.qdrant.yml up -d
        else
          echo "Qdrant is already running"
        fi

    - name: Clean publish folder
      run: rm -rf /opt/knowledge-api/out/*

    # 3️⃣  Publish the API (self-contained linux-x64)
    - name: Publish Knowledge.Api
      run: |
        cd Knowledge.Api
        dotnet publish -c Release -r linux-x64 \
                       --self-contained true \
                       -o /opt/knowledge-api/out
      # runner user (chatapi) must have write access to /opt/knowledge-api/out

    # 4️⃣  Restart systemd service
    - name: Restart service
      run: |
        sudo systemctl restart knowledge-api.service
        sudo systemctl --no-pager status knowledge-api.service

    # ========== MCP Server Deployment ==========
    # Note: /opt/knowledge-mcp/out must be created manually with:
    #   sudo mkdir -p /opt/knowledge-mcp/out
    #   sudo chown -R chatapi:chatapi /opt/knowledge-mcp

    # 5️⃣  Clean and publish MCP
    - name: Clean MCP publish folder
      run: rm -rf /opt/knowledge-mcp/out/*

    - name: Publish Knowledge.Mcp
      run: |
        cd Knowledge.Mcp
        dotnet publish -c Release -r linux-x64 \
                       --self-contained true \
                       -o /opt/knowledge-mcp/out

    # 6️⃣  Setup MCP systemd service (one-time creation)
    - name: Setup MCP systemd service
      run: |
        if [ ! -f /etc/systemd/system/knowledge-mcp.service ]; then
          echo "Creating knowledge-mcp.service..."
          sudo tee /etc/systemd/system/knowledge-mcp.service > /dev/null <<'EOF'
        [Unit]
        Description=Knowledge Manager MCP Server
        After=network.target knowledge-api.service
        Requires=knowledge-api.service

        [Service]
        Type=notify
        User=chatapi
        WorkingDirectory=/opt/knowledge-mcp/out
        ExecStart=/opt/knowledge-mcp/out/Knowledge.Mcp --http
        Restart=on-failure
        RestartSec=10
        KillMode=process

        Environment="ASPNETCORE_ENVIRONMENT=Production"
        Environment="ASPNETCORE_URLS=http://+:5001"
        Environment="DOTNET_RUNNING_IN_CONTAINER=false"

        [Install]
        WantedBy=multi-user.target
        EOF
          sudo systemctl daemon-reload
          sudo systemctl enable knowledge-mcp.service
          echo "✅ Service created and enabled"
        else
          echo "ℹ️  Service already exists, will restart"
        fi

    # 7️⃣  Restart MCP service
    - name: Restart MCP service
      run: |
        sudo systemctl restart knowledge-mcp.service
        sleep 5
        sudo systemctl --no-pager status knowledge-mcp.service

    # 8️⃣  Verify MCP health
    - name: Verify MCP health
      run: |
        echo "Waiting for MCP server to start..."
        for i in {1..30}; do
          if curl -f http://localhost:5001/health 2>/dev/null; then
            echo "✅ MCP server is healthy"
            exit 0
          fi
          echo "Attempt $i/30: MCP not ready yet..."
          sleep 2
        done
        echo "❌ MCP server failed health check"
        echo "Recent logs:"
        sudo journalctl -u knowledge-mcp.service --no-pager -n 50
        exit 1
